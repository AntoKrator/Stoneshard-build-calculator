/**
 * Single source of truth for the app version, shown in the UI and embedded in
 * shared build codes for forward-compatibility checks.
 */
export const APP_VERSION = '0.1.0'

/** The Stoneshard game patch this dataset currently targets. */
export const TARGET_GAME_VERSION = '0.9.4.x'
